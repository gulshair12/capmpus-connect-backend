/**
 * Socket.io or WebSocket setup can go here.
 * Example: attach to the same HTTP server in src/index.js when needed.
 */
// import { Server } from "socket.io";
// export const initSocket = (httpServer) => {
//   const io = new Server(httpServer, { cors: { origin: "http://localhost:5173" } });
//   return io;
// };
export default {};
